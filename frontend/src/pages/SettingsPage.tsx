import { useEffect, useMemo, useState } from "react";
import { KeyRound, PlugZap, Save } from "lucide-react";
import type { ConnectorDefinition, PageProps } from "../lib/types";
import { apiJson } from "../lib/api";
import StatusChip from "../components/common/StatusChip";

const CONNECTORS: ConnectorDefinition[] = [
  { id: "github", name: "GitHub", category: "Code", reliability: "high public", legal_note: "Official API/BYOK public code and profile search.", requires_key: true },
  { id: "hibp", name: "HaveIBeenPwned", category: "Breach", reliability: "authoritative", legal_note: "Official API only. Disabled without key.", requires_key: true },
  { id: "urlscan", name: "URLScan", category: "Infrastructure", reliability: "high public", legal_note: "Official API/BYOK or public lookups where allowed.", requires_key: true },
  { id: "virustotal", name: "VirusTotal", category: "Infrastructure", reliability: "high aggregator", legal_note: "Official BYOK connector.", requires_key: true },
  { id: "shodan", name: "Shodan", category: "Infrastructure", reliability: "high scanner", legal_note: "Official BYOK connector.", requires_key: true },
  { id: "censys", name: "Censys", category: "Infrastructure", reliability: "high scanner", legal_note: "Official BYOK connector.", requires_key: true },
  { id: "securitytrails", name: "SecurityTrails", category: "DNS", reliability: "high aggregator", legal_note: "Official BYOK connector.", requires_key: true },
  { id: "google_maps", name: "Google Maps/Places", category: "Maps", reliability: "official", legal_note: "Place enrichment only from analyst-supplied public place/profile evidence.", requires_key: true },
  { id: "intelx", name: "IntelX", category: "Breach/Data", reliability: "third-party", legal_note: "Official BYOK connector only.", requires_key: true },
  { id: "opencorporates", name: "OpenCorporates", category: "Corporate", reliability: "public registry", legal_note: "Public corporate registry enrichment.", requires_key: false },
];

export default function SettingsPage({ token }: PageProps) {
  const [settings, setSettings] = useState<Record<string, any>>({ llm: { provider: "local", endpoint: "http://localhost:11434", model: "llama3.1" }, api_keys: {}, connectors: {} });
  const [saved, setSaved] = useState(false);
  const [filter, setFilter] = useState("all");
  useEffect(() => { apiJson<any>("/api/v1/settings", undefined, token).then((payload) => setSettings(payload.data.settings || settings)).catch(() => undefined); }, [token]);
  const update = (path: string[], value: string | boolean) => setSettings((current) => { const next = { ...current, llm: { ...(current.llm || {}) }, api_keys: { ...(current.api_keys || {}) }, connectors: { ...(current.connectors || {}) } }; let target = next as any; path.slice(0, -1).forEach((part) => { target[part] = { ...(target[part] || {}) }; target = target[part]; }); target[path[path.length - 1]] = value; return next; });
  const save = async () => { await apiJson("/api/v1/settings", { method: "PUT", body: JSON.stringify({ settings }) }, token); setSaved(true); window.setTimeout(() => setSaved(false), 1800); };
  const rows = useMemo(() => CONNECTORS.filter((item) => filter === "all" || item.category === filter), [filter]);
  const categories = ["all", ...Array.from(new Set(CONNECTORS.map((item) => item.category)))];
  return <section className="settings-page premium-page"><header className="page-header premium-page-header"><div><span className="micro-label">Connector Center</span><h1>BYOK, LLM Engine, and Rate Limits</h1></div><button className="nx-primary" type="button" onClick={save}><Save size={15} />Save Settings</button></header><div className="settings-grid premium-settings"><section className="command-card premium-card"><header><KeyRound size={16} /><strong>AI Engine Config</strong></header><label>Provider<select value={settings.llm?.provider || "local"} onChange={(event) => update(["llm", "provider"], event.target.value)}><option value="local">Rule-based local</option><option value="ollama">Ollama</option><option value="openai">OpenAI compatible</option><option value="anthropic">Anthropic</option></select></label><label>Endpoint<input value={settings.llm?.endpoint || ""} onChange={(event) => update(["llm", "endpoint"], event.target.value)} placeholder="http://localhost:11434" /></label><label>Model<input value={settings.llm?.model || ""} onChange={(event) => update(["llm", "model"], event.target.value)} placeholder="llama3.1" /></label><label>Remote API Key<input type="password" value={settings.llm?.api_key || ""} onChange={(event) => update(["llm", "api_key"], event.target.value)} /></label><p className="muted-copy">Keys are stored through the backend settings endpoint. Masked values are preserved if the backend returns masked keys.</p></section><section className="command-card premium-card connector-center"><header><PlugZap size={16} /><strong>OSINT Connectors</strong><select value={filter} onChange={(event) => setFilter(event.target.value)}>{categories.map((item) => <option key={item} value={item}>{item}</option>)}</select></header><div className="connector-list">{rows.map((connector) => { const configured = Boolean(settings.api_keys?.[connector.id]) || !connector.requires_key; const enabled = settings.connectors?.[connector.id] !== false; return <article className="connector-card" key={connector.id}><header><strong>{connector.name}</strong><div><StatusChip label={enabled ? "enabled" : "disabled"} tone={enabled ? "ok" : "muted"} /><StatusChip label={configured ? "configured" : "requires key"} tone={configured ? "ok" : "key"} /></div></header><p>{connector.legal_note}</p><div className="connector-fields"><label><span>Enabled</span><input aria-label={`${connector.name} enabled`} type="checkbox" checked={enabled} onChange={(event) => update(["connectors", connector.id], event.target.checked)} /></label>{connector.requires_key && <label><span>API Key</span><input type="password" value={settings.api_keys?.[connector.id] || ""} onChange={(event) => update(["api_keys", connector.id], event.target.value)} placeholder="stored by backend" /></label>}<button type="button" onClick={() => update(["connectors", `${connector.id}_last_tested`], new Date().toISOString())}>Test</button></div><small>{connector.category} / {connector.reliability}</small></article>; })}</div></section></div>{saved && <div className="save-toast">Settings saved.</div>}</section>;
}

